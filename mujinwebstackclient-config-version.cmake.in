set( PACKAGE_VERSION "@MUJINWEBSTACKCLIENT_VERSION@" )
if( "${PACKAGE_FIND_VERSION}" VERSION_EQUAL "@MUJINWEBSTACKCLIENT_VERSION@")
  set(PACKAGE_VERSION_EXACT 1)
  set(PACKAGE_VERSION_COMPATIBLE 1)
endif()

if( "${PACKAGE_FIND_VERSION}" VERSION_LESS "@MUJINWEBSTACKCLIENT_VERSION@")
  set(PACKAGE_VERSION_COMPATIBLE 1)
endif()
